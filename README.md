# MaestrWave

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

MaestrWave 是一个面向“AI 生成管弦乐素材 + 体感指挥演绎”的 Web 应用。项目当前以 React + Vite 作为前端，FastAPI 作为后端，使用 ACE-Step 1.5 进行音频生成，并把手机陀螺仪/加速度计作为实时指挥输入。

## 项目定位

这个项目包含两个相互连接的阶段：

1. 生成阶段：用户描述风格、调性、节奏与时长，系统为不同乐器生成独立音频片段。
2. 演绎阶段：在浏览器或手机端通过传感器控制各声部的激活度、力度与混音，让生成出的音乐以“指挥”方式实时展开。

## 当前能力

- 基于项目模型组织“项目 → 乐器 → take”的生成流程
- 支持单乐器生成、重新生成与局部重绘
- 提供浏览页波形与统一播放体验
- 支持手机 IMU 作为实时混音输入，可选"手机自采自放"或"手机遥控、电脑出声"两种模式
- 生成后端可切换：本机 ACE-Step，或腾讯音乐天琴云端 API（不占本机显存）
- 当 ACE-Step 不可用时，自动回退到程序化占位音频，便于本地验证

## 一键启动包（GitHub Actions 自动构建）

通过 `.github/workflows/release-build.yml`，GitHub Actions 会把项目自动打包成
**解压即用、双击启动**的绿色发布包（内置 Python/Node 运行时，无需安装环境、无需命令行）：

- **手动触发**：仓库 Actions 页 → `Build & Publish Startup Package` → Run workflow，
  生成 `MaestrWave-macOS.zip` 与 `MaestrWave-Windows.zip`（可在 Actions 页下载）。
- **打 tag 自动发布**：推送 `v*` 标签（如 `v1.0.0`）时自动构建并发布到 GitHub Releases。

发布包结构：

```
MaestrWave-{平台}.zip
├── MaestrWave/                  # PyInstaller 打包的后端（FastAPI + uvicorn）
├── frontend/dist/               # 前端构建产物（vite build）
├── output/                      # 运行时数据（生成的项目与音频）
├── config.env                   # 可选配置（TME 密钥 / 端口）
├── Start-MaestrWave.command(.bat)  # 双击启动，自动打开浏览器
└── README.txt
```

使用方式：解压 → 双击 `Start-MaestrWave.command`（macOS）/ `Start-MaestrWave.bat`
（Windows）→ 浏览器自动打开 `http://localhost:3000`。

音频生成后端在发布包里默认使用占位音频（演示链路完整可用）；如需腾讯天琴云端真实
生成，用文本编辑器在 `config.env` 填入 `TME_APP_ID` / `TME_APP_KEY` 并将
`GENERATION_BACKEND` 设为 `tme` 即可——同样不需要命令行。若仓库为私有且信任下载者，
也可在手动触发时勾选 `tme_inject`，由 Actions 从仓库 Secrets 注入密钥。

> 实现要点：后端打包由 `scripts/package/entry.py` + `maestrwave.spec` 完成，前端
> dist 在入口脚本中显式重定向给 `backend.app`（不改动任何业务代码）；CI 里带一次
> 真启动冒烟测试（HTTP 200），确保发布包开箱即用。

## 版本记录

| 版本 | 日期 | 变更内容 | 类型 |
|------|------|----------|------|
| M7l | 2026-08-12 | **两首真实交响乐接进代码**，考试与指挥体验都能选。新增 `backend/midi_in.py`（读 SMF → 项目现有的 `(blueprint, parts)`，自带最小解析器不引入 mido）与 `backend/repertoire.py`（曲目清单、渲染缓存、造可指挥的项目）。渲染产物与练习曲**同形同目录**，所以 `/api/practice/{id}` 的状态/音频/MIDI 三条端点原样服务这两首，前端 `PiecePlayer` 与 `PracticeRunner` 一行没改。三条设计上的硬决定：①**绕开 `validate_and_repair_part`** —— 它是给语言模型输出兜底的（量化到 1/4 拍、丢重叠音、melody 复音砍到 2），真实管弦乐声部过一遍会被打残；绕开对现有生成链路是零改动，放宽则会改变所有 LLM 项目的行为。②**不夹音域**：库里没有 bassoon，最接近的 `woodwind` 是 55–93 而大管真实音域是 34–75，照 `score.py` 的规矩夹会把整个大管声部搬高一个八度；真实总谱的音高按定义就是对的。③**音色号与 `library_key` 分开给**：这两份文件的圆号小号都被 LilyPond 写成 GM 69（英国管），轨道名又只是 `one:` `two:`，所以声部表按总谱顺序写死。**选段用 tick 而不是小节号**：埃格蒙特前面是 3/2 与 3/4，尾声那段 4/4 的起点除以 4/4 小节长是 232.5，照小节号截会从半个小节中间切进去、强拍整段错到第 3 拍上（实现时才发现）。贝七全轨 velocity 恒为 127，按「织体权重 + 音符密度、三小节移动平均」反推力度曲线，并**同时用于渲染**（只当评分真值而音频是平的，等于让人对着没有强弱的音乐做渐强），另叠 `_beat_accent` 恢复小节内层次；反推结果重现了这一乐章公认的形状（前 10 小节均值 0.44 → 后 10 小节 0.87，阶跃落在小提琴加对旋律那一小节），这条单调性断言进了 `repertoire.self_check()`。埃格蒙特有 85 级真实力度，原样用不反推。考试新增第 4 张考卷（力度只占 15%，因为曲线是推导的；界面注明「从配器反推」且只在**已知**时才说，未加载时不作断言）；文件页新增「内置曲目」与已有项目并列，点开逐轨渲染成 take。**验收**：两首在 fluidsynth / sf2 / builtin 三条渲染路上时长完全一致、首末非静音位置一致、能量包络相关性 0.86–0.93；`/api/repertoire/{id}/source.mid` 下载的 sha256 与 `assets/` 完全一致（未截取）。**回归**：`score.py`/`practice.py`/`render.py`/`project.py`/`midi_out.py`/`configuration.py` 一个字没动，08-12 之后渲染的既有曲目 piece_id 未变且 MIDI 字节一致（08-11 那批是 M7k 加 `_ALGO_VERSION` 时预告过的孤儿），课程页练习曲链路照常 | feat |
| M7k3 | 2026-08-12 | 收两首交响乐 MIDI 作模版曲目，放 `assets/repertoire/`（**只是素材，没动代码** —— `midi_out.py` 只有导出没有导入，后端还读不了 MIDI）。**考试**取贝多芬第七 Op.92 第二乐章，**指挥体验**取贝多芬《埃格蒙特》序曲 Op.84，两份都是 Mutopia 的 **Public Domain（CC0）**那一档。选源的理由：*曲子是公有领域不等于那个 MIDI 文件是* —— MIDI 序列本身是有版权的演绎，多数古典 MIDI 站没有明确授权；Mutopia 每个文件单独标注许可。刻意避开 CC BY-SA 的条目（德沃夏克第九《自新大陆》就因此排除）：BY-SA 是传染性的，进了仓库就得一直挂着并署名，不能被 MIT 吸收。逐个文件解析核对过而不是照标题选，因此发现三件事：①第七第二乐章**拍号只有 2/4、速度事件只有一个值 76 BPM**，正好绕开「全曲只支持一个速度」这条限制，但**所有音符 velocity 都是 127**（LilyPond 未写 `\dynamics` 的默认导出），所以它只适合做不考力度的考卷；②《埃格蒙特》有 **85 种 velocity（12–127）**，是全部 PD 候选里力度信息最丰富的，适合体验模式；③两份文件的**铜管 GM 音色都是错的**（圆号小号写成 GM 69 英国管），轨道名也只是 `one:` `two:`，接入时只能按总谱顺序认声部。另记下两个照标题会踩的坑：Mutopia 的「Ein Sommernachtstraum No. 5」不是婚礼进行曲（那是第 9 首），《魔笛》序曲的 `MFO-Score.mid` 只有 48 秒是片段。备选与实测数据写在 `assets/repertoire/README.md` | chore |
| M7k2 | 2026-08-11 | **拍点认得更全**。先把量的范围补上：原来定参只扫了 2/3/4 拍 × 66/88/120 BPM × 三档抖动，拍型大小固定 —— 而真人不是照着标尺挥的，所以另列一组极端条件（拍型只占画面 0.2、抖动 σ=0.016、反弹压到 35%、15fps）。一量就看出短板不在常态而在这里：常态查全 95.6%，极端只有 89.0%、查准 86.7%。改了两个数。（1）**简化阈值 `epsilonRatio` 0.10 → 0.18**。（2）**不应期从拍长的 45% 提到 60%**（`ConductingModel.setBaseBpm`）—— 这一个数**同时买到查准和查全**：一个误报落在真拍点前面时，不应期会顺手把紧跟着的真拍点也吞掉，一次误报制造两处错，所以压掉误报本身就在救漏检。合计：常态 95.6%/96.9% → **97.1%/99.6%**，极端 89.0%/86.7% → **96.6%/99.2%**，拍点时刻偏差 19ms → 17ms；逐条看，拍型小的从 81% 到 95%、抖动大的 76% → 95%、反弹扁的 83% → 96%、15fps 的 89% → 98%，每一格都在 95% 以上。（3）配套改「低角」判据：原来是「比两侧顶点都低」这个**零余量的严格不等式**，简化阈值提上去之后必须换 —— 简化会吃掉一些反弹顶点，于是拍点的邻居常常是**另一个拍点**、高度本就相当，严格式当场失效（实测保留严格式的话查全从 97% 掉到 84%）。改成「低于两侧的中点」，扫下来这条轴在 −0.08~0 之间是平的，取 0，不配魔数。**记下三件别再浪费时间的事**：`minEdgeRatio`（≤0.18）与 `minTurnDeg` 现在完全不起作用（0.10 和 0.18、20° 和 50°，数字一模一样），简化够狠时活下来的顶点本来就边长够转角够；不应期**不能再往上加**，它是拿曲子 BPM 算的而不是用户实际速度，70% 时抢拍 25~40% 的人查全率直接掉到 80%（60% 还有 98.8%）；代价是确认滞后 122ms → 176ms，只影响画面上什么时候亮，不影响拍点时刻。原有 28 条断言全部通过，端到端各条件覆盖率均为 43/44 拍 | fix |
| M7k1 | 2026-08-11 | 添加 MIT License：根目录 LICENSE（Copyright 2026 Diankun Gao, Xingwen Zhao），README 加徽章与 License 章节，package.json 标注 license 字段 | chore |
| M7k | 2026-08-11 | **练习曲听不出是几拍子**，以及跟练时不知道该怎么打。（1）先量后改：对重音包络做自相关（重音的周期就是听觉上的拍号），进行曲四拍子的 lag=2 是 +0.82、lag=4 是 +0.92 —— **只差 0.10，等于没有拍号**，抓错强拍数成三拍完全正常。根因在 `_vel`：每一拍的轻重被折进 `dyn` 再过 `0.42 + 0.85·dyn` 那个下限，写着「弱拍 68%」实际算出来是 82%，耳朵听不出；而四拍子压根没写次强拍。改为 `accent` 乘在下限**之外**，并给出强-弱-次强-弱的层次（`_beat_accent`）。次强拍取 0.62 是扫出来的，**上限比下限更要紧**：加重到 0.82 反而更糟（lag4−lag2 从 +0.23 掉到 +0.15），一小节四拍会听成两小节二拍；同理次强拍上**不补大鼓** —— 大鼓是全曲最低最响的一件，它落在哪儿哪儿就是小节头，第 1、3 拍各来一下等于每两拍宣告一次小节开始（掉到 +0.09），第 3 拍该由**音色缺席**来区分。另：强拍不再被拆成两个八分（拆开正好抹掉唯一的定位点），数拍小节的强弱拉得比正曲更开（那是用户唯一一次能从容听拍号的机会）。改完 lag1/2/3 全部落到 +0.67 以下、lag4 保持 +0.91。（2）`piece_id` 拌入 `_ALGO_VERSION`：缓存键原本只由 spec 算，换了作曲逻辑之后 id 一模一样、磁盘上那份旧音频会被继续端出来，而且两台跑着不同版本的后端会在同一个 id 下给出不同的曲子 —— 「同一个 id ⇒ 同一份音频」现在才是真的。（3）跟练页开场先报**这一首是几拍子**（4/4 徽标 + 每小节几拍 + 走向口诀 + 数拍几小节几声），拍号错了整段白打，而听出拍号不该是初学者在跟练时顺便完成的任务。（4）**拍型小窗**压在摄像头画面角上（视线离开画面去别处对照，手就跟着歪了），可收起；光点由**真实拍网格**驱动而不是自己按 BPM 转 —— 两个时间源几十秒下来必然错开，那时它指的就是另一拍了；数拍期间停在预备位置。顺带修：`countInBarsFor` 对 1/2 拍给的是两小节，而界面三处写死「一小节数拍」，且节拍器退路固定给一小节 —— 同一课「跟曲子」和「跟节拍器」的起手时机不一样，用户只会以为是自己没数准 | feat |
| M7j2 | 2026-08-11 | 拍型准确度判松一点点：DTW 阈值 0.08/0.30 → **0.10/0.34**。松的理由是这一维只该回答「乐手能不能看出你在打第几拍」，不是描红，而位置准不准、快慢稳不稳另外四维已经分别在评；**不松到底**的理由是松过头它就只剩「匀速划圈」一种错误还扣得下去，等于白占一份权重。量出来的落点：打对了但不标准的样本（真人抖动、拍点挪 8~15%、反弹塌一半、整段越打越高）在 0.056~0.117，改后拿 93~100（原本 83~100）；真的打错的仍然扣得到 —— 四拍第 2 拍打反方向 0.130 → 88 分、该打二拍却打了三拍 0.145 → 81 分；匀速划圈 ≥0.365，照旧 0 分。**顺带记下这一维测不出什么**：「打错拍号」和「打对了但拍点挪了 8%」的距离本来就重叠（0.145 vs 0.117），归一化掉位置与大小之后各拍号的轮廓极像（模板两两之间只差 0.034），真正的区别是拍点落在这条线的哪几处，而那份信息正好在归一化时被丢掉了 —— 所以别指望靠收窄阈值把它逼出来，要诊断拍号打错得把拍点时刻一起喂进 DTW | fix |
| M7j | 2026-08-11 | 按手绘图重画 2/3/4 拍的图形拍型（`patterns.ts`）。三张图按圆点数对应拍号，坐标从「乐手看指挥」翻回指挥自己的左右，再按比例摆正到画布中央（横向不越界）—— 翻过来之后四拍正好是标准的「下 → 左 → 右 → 上」，三拍是「中下 → 右 → 中上」，与教材一致。**拍点改为逐拍升高**（第 1 拍最低、末拍最高），这是纸面图式的通行画法：几条弧线全挤在同一高度上就分不清谁压着谁；末拍到顶还兼作下一小节的预备。二拍那张图里右上角的圈**不是拍点而是预备弧的顶点** —— 它和第 1 拍横坐标只差 0.004，两点间是直线，正说明第 1 拍垂直落下，据此定出「第 1 拍中下、第 2 拍右」。示范图相应去掉「拍点平面」那条横线（拍点不再共面，一条横线只穿得过第 1 拍，标上四个字就是在教错），身体中线改为通高；拍点编号与走向文字都改为沿**法线朝图形外侧**让开 —— 四拍第 2→3 拍的横扫与第 4 拍落下的那一笔在中间交叉，两段中点几乎重合，固定偏移会把「向右」和「上提·预备」叠印成一团 | feat |
| M7j1 | 2026-08-11 | 配套改评分模板：`motionTemplate.ts` 的纵轴改为**重新构造**而非从图式换算。原来「保留反弹相对于弦的高度」那种等价变换对升高式图式行不通 —— 图上反弹顶点的高度大半来自「下一个拍点本来就更高」，把拍点压平后这份高度就没了，实测三拍预备段算出来的抬手量正好是 0，也就是「预备拍完全不抬手」，而预备恰恰是全小节抬得最高的一下。改为只继承横向走向，纵向按动作本身写死（反弹 +0.10、预备 +0.72）。实测：一个拍点全落在平面上（教材做对）的人，用动作模板是 100/100/100，直接拿新图式当模板则是 88/60/72，即做对的人凭空丢 12~40 分。端到端总分维持 95~98，「拍点清晰度」从 56~80 升到 68~89。课程里「所有拍点落在同一个高度上」那条要点改写为图与手的区别（图上升高是为了看清，手要落回同一平面），否则它和旁边的示范图正好相反 | fix |
| M7i1 | 2026-08-11 | **修「摄像头一开，整层标记就消失」**：视频元素是 `wrap.appendChild(video)` 塞进来的，排在 canvas **后面**，而两者都 `position: absolute` 且都没有 `z-index` —— 绘制顺序就是 DOM 顺序，视频把整层严严实实盖住。这不是新引入的：光迹、光点、席位分区线，凡是画在这层上的东西，**只要摄像头真的在跑就一直是看不见的**，而且完全不报错，光看代码也读不出来。给 `.overlay` 加 `z-index: 1`（placeholder 给 2）。定位办法是在离线验证台里给假 source 挂一个占位「视频」元素，走的是和真机一模一样的 `appendChild` 路径，一挂上整层立刻消失，一加 z-index 立刻回来。顺带补一条提示：只认到表情手时，画面上只有一个小绿点、既没光迹也没光点，看起来和「功能坏了」一模一样，现在明说是打拍手没进画面（并提示惯用手相反的话去开「交换双手」） | fix |
| M7i | 2026-08-11 | 摄像头画面上加一层实时反馈，解决「手认得挺准，人却是懵的」：**光迹**（打拍手走过的路留 700ms 渐隐拖尾，视觉语言和首页的 `ConductingTrace` 一致 —— 一个点只说明「有东西在动」，尾巴才把手势的**形状**留在画面上）、**拍点闪光**（确认一拍时光点变亮变大，回答「刚才那下算数了吗」）、**水波纹**（从拍点扩散出去的两圈涟漪，替掉原来那个「整个画面描一圈边」—— 波纹**有位置**，用户能看出软件认为拍点落在哪，落歪了自己就看得见）。两处细节：波纹的**位置**取自拍点真正发生的那一刻（拐角要晚约 120ms 才确认，照确认帧画会明显冒在拍点后面），但**动画时钟**从看见它的那一刻起算（否则 620ms 的波纹一出场就放完了五分之一，闪光只剩 80ms）；一只手都没认到时画面中央明说，别让人对着黑屏猜。另修两处：光点原本画在光迹缓存的末端，摄像头一卡缓存清空、光点就凭空消失，改为直接画在当前手的位置；拖尾的渐隐曲线原样照抄自首页（纯黑画布上调的 `k²`），压在摄像头画面上尾段只剩 0.05~0.15 的透明度、基本看不见，改成线性渐隐加下限、并换用更亮的 `--wave-4` | feat |
| M7h | 2026-08-11 | 合入上游关于课程与动画的两次提交，按「课程与动画取上游、其余本地优先」处置。**取上游**：新的一拍图式（斜椭圆，`Meter` 扩到 1）、拍点回到同一平面、`rebound` 命名、示范组件、课程内容（一拍课移进单元一、左手课重写并新增三个维度、要点与常见错误改写）。**保留本地**：评分标定、延迟校准与学习反馈、练习曲/考试链路 —— 上游那两次提交是在 M7e 之前的文件上做的，直接合会把 `curriculum` 的 `music` 规格退回 `practicePrompt`、把课程页的练习曲接线整段删掉，那是功能回退不是课程内容，所以按**内容**合而不是按文件合。三处接缝：①`motionPattern` 被上游删掉，改放 `lib/teaching/motionTemplate.ts` —— 图式把末拍画在 0.84（那一拍同时是下一小节的预备，纸面上没问题），直接拿它当 DTW 模板的话，一个拍点全落在平面上、也就是**完全做对**的人三拍丢 15 分、四拍丢 24 分；现在评分侧只从图式派生一个「拍点压回平面」的视图，横向与反弹一个数都不动，图式改了模板跟着改。②**一拍图式的相位是错的**：`beat = 0` 返回椭圆顶点而不是拍点，和模块自己三行以上写的约定相反，而示范的拍号显示、DTW 模板起点、拍点清晰度的取样窗口全都依赖它；整体挪一个 SPLIT 修正，几何没动。③后端接受 `meter=1` 并给一拍补上铺底与旋律的分支，数拍小节数改为按拍号给（1/2 拍给两小节，否则只响一两声人来不及反应）。顺带修：上游新增的三个维度没有实现，而左手课的 rubric 只有这三维，旧代码会把它们静默 `filter` 掉并算出「0 分 + 一张空表」；现在照常列出并标明「尚未实现」，`scorable` 为假时讲评页不显示分数与等第 | fix |
| M7g | 2026-08-11 | 围绕「学会打拍」补三样东西。**音画延迟校准**（`lib/teaching/latency.ts` + 设置页 / 考试自检）：跟着咔哒声敲十下，量出「你 + 这套设备」的固定偏移并从拍网格里减掉 —— 蓝牙耳机 150~250ms 的链路延迟 `AudioContext.outputLatency` 报不出来，不校准的话每一拍都被判成拖拍（实测 200ms 延迟就能把这一维打到 79 分，修容差之前是 0 分）；实测灌入 +80ms 的偏移，测得 +78ms。**跟练时的即时偏差指针**：每打一拍立刻显示早了/晚了多少，中间一段是容差区 —— 学节拍靠的是闭环，一轮打完才说「平均晚了 60ms」是没法照着改的。**讲评页的逐拍时间线**：一拍一根柱子，上=拖下=抢、小节第一拍加深、没打出来的标红，「每小节第 1 拍都抢」这种规律一眼看得见。另加两条提示条压在分数上面：疑似设备延迟、以及**拍型太小**（占画面不到 22% 时，检测信噪比垮掉会同时拖垮三个维度，实测 46/50/67 —— 与其让三条评语各说各的，不如直接说「打大一点」） | feat |
| M7f | 2026-08-11 | **分数太低：先修 bug，再谈宽容度。** 量了一遍才发现三件事混在一起。（1）**「速度稳定性」在量检测噪声，不是在量速度**：拿同一段轨迹分别用真值拍点和检测拍点评分，真实速度波动恒为 CV 3.6%，按相邻拍算出来却是 4.7%~26.7%，抖动大时八成以上是噪声，这一维被打到 **8~24 分** —— 而那份噪声在「拍点准确度」里已经罚过一次。改成跨小节取间隔（噪声摊薄到 1/meter），并补一个**漂移**项（前后段拍长之差）：匀速加速的人 CV 只有 9%、照 CV 算还能拿 84 分，而这恰恰是这一维最该抓的毛病。漂移在**用户自己的拍点序列**上算 —— `matchIctus` 会跟着漂移重新配对，把 30 秒加速 20% 测成 0.1%；同时减掉漂移自身的采样噪声（`2σ`），免得把手抖判成越打越快。（2）**容差低于测量下限**：满分线原本是 5% 拍长 = 34ms，而摄像头一帧 33ms、检测器自身偏差 19ms，等于要求用户抵消我们的误差；改成 7% 且不小于 45ms，并按人跟拍的实际水平（非音乐专业异步量 SD 30~50ms）定线。（3）**拍点偏差里混了设备延迟**：拆成「围绕自己中心的离散程度」（主项）与「系统性偏移」（有 70ms 免罚区、封底 0.55 的乘数），后者大而整齐时判为音频延迟并明说。标定台十一种典型样例：老手 99 / 认真的初学者 97 / 紧张的初学者 90，而越打越快 70、匀速划圈 38、拍型缩成一小团 65 —— 放宽的同时没放过任何一种真毛病 | fix |
| M7e1 | 2026-08-11 | 三拍图式改成**凹三角形**：三条弧全部往里凹（途经点移到弦与重心之间），三个拍点因此成为尖角。原来三条边都鼓出去，拍点被弧线抹圆，图上看不出拍打在哪 —— 而这张图存在的全部理由就是告诉人「拍在这三个位置」；顺带和二拍（兜一个大圈）区分开了。拍点端点一个没动。评分模板跟着受益：一个打得完全正确的人在三拍上的拍型分从 93 升到 100（内凹后的横向位置更接近真人的反弹路线）。断言从 137 条加到 143 条，凸性判据换成「途经点必须和重心同侧、且凹陷看得出来（离弦 > 2% 画布）」 | fix |
| M7e | 2026-08-11 | **「开始考试」能点了，练习曲改用 MIDI 写谱渲染**（M6 第 4 步，做法与计划不同）。新增 `backend/practice.py`：照规格直接写音符（旋律/铺底/低音/定音鼓/打击乐五个声部，管弦乐编制，鼓件只用 `DRUM_KEYS` 那七件），走 M7 的 `render` 渲染再混成一条音轨，同时导出 MIDI；`POST /api/practice/generate` + 三条读取端点，spec 的 sha1 就是 `piece_id`，**同一份规格永远渲染出同一份字节**。原计划是调天琴 `text2music` 再对音频做能量起始点检测找拍网格相位、置信度低时让用户跟拍手动校准 —— 换成写谱之后这一整套都不需要了：拍网格是**写下的**，第一拍精确落在数拍结束那一刻（实测把网格前后各挪 60ms，拍点处的能量跳变从 3190× 掉到 0.9×）。另外两个白捡的好处：考试曲目不用往仓库里塞音频（`exam.ts` 里三个 `audio: null` 换成 `music` 规格，「所有人考同一首」由规格是常量 + 渲染可复现保证），以及**「力度对应」这一维终于评得了** —— 每小节的力度是谱面上写下的数，不是从音频测的响度近似值；`splitBars` 顺带带出小节号，漏打开头两小节不会再把第 3 小节的动作拿去比第 1 小节的力度。前端：`PracticeRunner` 多一个声源（`PiecePlayer`，起播锚点与输出延迟补偿照抄节拍器那套），曲子没好就退回节拍器；课程页进页面即渲染，考试页选曲即渲染、自检里多一盏「曲目就绪」灯、打满小节自动出分并按 70/85 判优秀与及格。**没做**：全曲只支持一个速度，所以三级曲目原本要考的渐慢去掉了（要动 `score.py` 的时间换算，那是项目链路的公共代码），`exam.ts` 里如实改了描述而不是留一首「说是渐慢其实匀速」的曲子 | feat |
| M7d1 | 2026-08-11 | 图式与评分模板分开：`patterns.ts` 新增 `motionPattern()`，把画给人看的图式**只压纵轴**成「手实际怎么动」—— 拍点全部落回同一平面、拍与拍之间改为向上反弹、末拍之后那一段（预备拍）抬得最高；横向走向原样保留（那是两者共有的真正信息，也是乐手数拍的唯一线索，图式改了左右模板跟着改，不会漂移）。DTW 模板与「拍点清晰度」的满分基准改用它。纸面图式把拍点逐拍抬高是为了路线画得开，真手不这么走 —— 混为一谈的代价是可量化的：一个打得完全正确的人（拍点在平面上）拿图式比，2/3/4 拍只有 65/42/60 分，换动作模板后是 84/93/100，端到端总分从 54~79 升到 83~96。断言加到 137 条，含拍点多边形的形状（三拍是凸三角形；四拍必然自交 —— 路线是中→左→右→中，那个交点正是它和三拍最好认的区别） | fix |
| M7d | 2026-08-11 | 外部符号音乐模型接得上了（M7 第 4 步）：写出 `docs/SYMBOLIC_COMPOSER_API.md`（代码里一直引用它但文件不存在）—— 一个 `POST /compose_part`，含请求/响应字段、音符定长数组的理由、五条修复规则「你不用管」的边界、管弦乐打击乐的七个合法鼓件、失败语义与一段能跑的最小实现；「设置」页加作曲器四选一与服务地址输入，地址落后端偏好文件（`SYMBOLIC_COMPOSER_URL` 降级为默认值）。**接口只接受本机/局域网地址**，公网地址必须走环境变量 —— 隧道一开后端就在公网上，这个地址决定了整份乐谱被 POST 到哪，照抄 `llm.py`「白名单只能手工加、不能通过接口加」那条线。顺带修掉 `POST /api/score/prefs` 在包模式下必然 500 的 bug（函数里 `from config import …`，而发布包正是以 `backend.app:app` 启动的，等于设置页的音源选择在发布包里从来没work过） | feat |
| M7c1 | 2026-08-11 | **重写拍点识别：轨迹化成多边形取拐角**（`lib/camera/ictusDetector.ts`）。原来按逐帧「垂直速度过零点」判拍，而过零点是噪声的属性不是手势的属性 —— 合成真人轨迹实测：正常挥 30 秒（应 44 拍）数出 79~121 拍，间隔中位数 100~133ms，也就是三四帧一「拍」；`MIN_BEAT_INTERVAL_MS` 只拦了 BPM 估计，没拦拍点本身。新做法先用 Douglas–Peucker 把轨迹简化成折线（抖动小于阈值，整串假过零点在这一步消失），再按「两条邻边够长 + 转角够大 + 是低角」挑拍点，二维判据所以横向折返的第 2、3 拍也认得出；不应期改成跟拍长走（慢速下固定 200ms 形同虚设，66 BPM 查准率 66%）。实测查全 93~96%、查准 95~98%、拍点时刻平均偏差 19ms。拍点时刻单独带出来（拐角要等出边长够才确认，晚约 120ms，直接用确认帧的时间会把每一拍都记成「拖拍」）。顺带修「速度稳定性」：漏一拍会产生一个双倍长间隔，几个就能把变异系数从 4% 抬到 25%，改成只按对上网格的拍算、并除以跨了几拍 | fix |
| M7c | 2026-08-11 | 写谱模式的谱面可见性（M7 第 3 步收尾）：新增只读钢琴卷帘 `components/PianoRoll/` —— 当前乐器实色、**其它声部淡色叠在后面共用一把音高刻度**（这样才看得出配合得上配合不上），小节线与段落分隔、力度映射到不透明度、播放头复用 `Waveform` 的 `getProgress` + rAF；「导出 MIDI」走 `<a download>`；`take.params` 里的修正条数、作曲降级原因、旧蓝图、实际重绘小节都如实显示成一行提示；写谱模式的 backendBar 改显示作曲器与音源；重绘按钮不再被 ACE-Step 的能力位挡住（那是另一条链路）；`api.projectScore()` / `scoreMidiUrl()`；项目导出 zip 带上 `scores/` 与 `score.mid` | feat |
| M7b3 | 2026-08-11 | 修两处波形显示：一是波形按绝对幅度画，而写谱模式为保住配器平衡是固定增益不归一化的（一条小提琴独奏轨峰值只有 0.067），画出来是一条贴着中线的直线 —— 改为按整轨最大值归一化，画形状不画绝对响度；二是 `Waveform` 只在依赖变化时画一次，首帧宽度为 0 时后备位图只有 1px 宽再被 CSS 拉开，屏幕上是一片糊掉的色块，补上 ResizeObserver（其它几个 canvas 早就这么做了） | fix |
| M7b2 | 2026-08-11 | 按手绘图式重画 2 / 3 / 4 拍示意图：拍点不再全部压在同一条平面上，改成第 1 拍最低、逐拍升高、末拍到顶，轨迹是几段光滑弧线（`rebound` 更名 `via`，它是曲线真正经过的途经点）；圆点标每一个整数拍，编号与走向词一律沿图形法线向外让开，四拍的「长扫」与「落下」不再叠成一团。手绘是乐手视角，仓库里一律存指挥自己的左右，因此左右整体翻转 —— 翻完正好与课程文字「二拍下→右上、三拍下→右→上、四拍下→左→右→上」对上。93 条离线断言：整数拍精确落点、闭合、逐拍升高、途经点真被经过、段内平滑、朝拍点加速、四拍的交叉点 | fix |
| M7b1 | 2026-08-11 | 发布包内置 fluidsynth 与音源：`scripts/package/bundle_fluidsynth.py` 递归收全部依赖并把 `install_name` 改写成 `@loader_path` 相对引用（macOS 实测 19 文件 5.7MB，全部文件零残留绝对路径，剥离环境下实跑通过），启动器用 `SOUNDFONT_DIR` 指向包内音源；「设置」页加音源选择（自动 / 内置采样 / FluidSynth / 内置合成），选择存后端偏好文件而非环境变量——双击启动包的用户改不了 env | feat |
| M7b | 2026-08-11 | **自带管弦乐音源，零安装出声**：`backend/sf2.py` 纯标准库的 SoundFont 解析与采样播放（分区选择、按音高重采样、循环点、音量包络、全局区继承），从 MIT 授权的 FluidR3_GM 裁出 33MB 只含本项目 12 个音色的 `orchestral.sf2` 进仓库（裁剪前后逐采样最大差 4.33e-07，低于 16-bit 量化噪声）；修掉两处会静默出错的问题——SF2 常用「硬左+硬右叠两层」写法，单声道直接叠加会响一倍，按 (L+R)/2 补上降混增益；音源缺鼓件时原本那一声凭空消失且不报错，加了替代链 | feat |
| M7a1 | 2026-08-11 | 修复中文项目名下载一律 500：HTTP 头只能是 latin-1，`filename="接口验证.mid"` 会让 starlette 编码响应头时抛 UnicodeEncodeError。改用 RFC 5987 的 `filename*`。**zip 导出一直有这个 bug**，一并修掉 | fix |
| M7a | 2026-08-11 | 写谱演奏模式接线：`score_gen.py`（蓝图带并发锁、生成、按小节重绘、MIDI 导出）、生成/重绘端点按 `generation_mode` 分流（URL 与返回结构不变）、`/score` 与 `/score.mid`、health 里的 `score` 块、新建项目时选生成方式；LLM 通路改用 `chat_text` 以便从被 `max_tokens` 截断的输出里抢救音符，任何失败都退回规则作曲并如实记录降级原因 | feat |
| M7 | 2026-08-11 | 写谱演奏模式的地基：`score.py`（乐谱结构、调式与和弦解析、五条确定性修复）、`midi_out.py`（手写 SMF type-1）、`render.py`（fluidsynth + 纯 Python 两个渲染器，固定增益不归一化、各声部采样数严格一致、尾音叠回开头做无缝循环）、`composer.py` 的规则作曲器（声部进行、音区分离、participation 落地、管弦乐编制的打击乐）；124 条离线断言含拍网格反查与循环接缝 | feat |
| M6h | 2026-08-11 | 新增首页并作为落地页：招牌用课程/评分共用的那份拍型轨迹画成指挥棒长曝光光迹，两扇门分别通向教学与体验（体验按五步流水线可直接点进任一步），内嵌助手与侧栏共享同一段对话；点侧栏品牌区回首页 | feat |
| M6g3 | 2026-08-11 | 修复跟练**必崩**：节拍器把数拍排在网格原点之前，而每次 `start()` 都新建 AudioContext（`currentTime` 恒为 0），88 BPM 下第一个数拍落到 -2.6 秒，`osc.start()` 抛 "startTime must be a positive value"。原点改为让出整段数拍时长 | fix |
| M6g2 | 2026-08-11 | 助手回复按 Markdown 渲染（自己拼 React 元素，不引 react-markdown、不碰 `dangerouslySetInnerHTML`——渲染的是模型输出）；接口错误改抠 FastAPI 的 `detail` 并带上状态码，404 直接提示「后端进程还是旧的，重启后端」而不是甩一句 `{"detail":"Not Found"}` | feat |
| M6g | 2026-08-10 | 右侧可折叠的对话式 Agent 侧栏：走已有 BYOK 通路，能答指挥知识与软件操作，上下文取自用户指南 + 课程数据 + 当前项目摘要 | feat |
| M6f1 | 2026-08-10 | 确定教学与考试纯用摄像头，砍掉手机传感器兜底（IMU 只有加速度、给不出手的位置，六维里三维评不了），回退已写的一半 IMU 采样通路 | chore |
| M6f | 2026-08-10 | 跟练录制与六维评分：Web Audio 严格节拍器兼作拍网格、录制层、DTW 图形拍型识别（M5 欠账）、六维打分与讲评页；补齐 `ConductingModel.reset()` 漏掉的状态 | feat |
| M6e | 2026-08-10 | 「指挥教学」下新增「考试」页：三首固定示例曲目 + 评分维度权重 + 可用的摄像头自检（环境检查／认手／帧率）；曲目音频与打分待第 5 步 | feat |
| M6d | 2026-08-10 | 指挥教学的课程与示范：11 课静态课程数据（单元一二三）、标准图形拍型几何、按 BPM 走的拍型动画、课程列表与单课页；M6 计划书进仓库 `docs/M6_PLAN.md` | feat |
| M6c | 2026-08-10 | GitHub Actions 一键打包发布：PyInstaller 打包后端 + vite 前端，生成 macOS/Windows 双击启动包，手动触发或打 tag 自动发 Releases；TME 密钥可经 Secrets 注入 | feat |
| M6b1 | 2026-08-10 | gitignore 掉 `frontend/node_modules/` 与 `frontend/dist/`（含全部 @fontsource 字体文件），从索引移除 3846 个依赖/构建产物，仓库仅保留源码与文档 | chore |
| M6b | 2026-08-10 | 导航改为两级：一级「指挥教学 / 指挥体验」，原有七页归入「指挥体验」，侧栏加宽到 196px 带文字；一级归属由 `activePage` 推导而非另存一份 | feat |
| M6a1 | 2026-08-10 | 新增 `docs/USER_GUIDE.md` 用户使用指南，为 GitHub 项目文档准备 | chore |
| M6 | 2026-08-10 | UI 全面改为衬线体（Source Serif 4 + Noto Serif SC，canvas 刻度统一走 `--font-mono` token），品牌图标更换为 MW 字标 + 海军蓝→黑渐变，补上 favicon 与 apple-touch-icon | milestone |
| M5 | 2026-08-10 | 摄像头指挥：MediaPipe 手部追踪 + 按指挥法标准的双手分工（打拍手管拍点与速度、表情手管力度与声部平衡），「输出」页新增第三种模式 | feat |
| M4d | 2026-08-05 | 新增「构型」页（BYOK 语言模型 + 乐曲情绪柱状图 + 5 个模版），项目数据加 schema 版本与惰性迁移，`total_duration` 成为正式字段 | feat |
| M4c1 | 2026-07-29 | 参数集中到 gestureConstants.ts，新增手势解析调试页（/?debug=conduct），修正起播基线收敛与收势重复触发，文档同步 | chore |
| M4c | 2026-07-29 | 声部逻辑重构为混音平衡范式：拆出与传感器无关的 ConductIntent 抽象，强调向量取代四轴 sigmoid，节奏声部改由拍点脉冲驱动，删除排序静音 | feat |
| M4b | 2026-07-29 | 指挥响应稳定性重构：力度改用活动包络+自适应滞回门限，重力基线自适应，收势去抖，音量写入合并为每轨每帧一次 | fix |

M4 之前的记录见下方历史条目。

- ✅ **M0** 仓库可运行状态修复：解决一次 merge 遗留在 `main` 里的未解决冲突标记（导致 `backend/*.py` 语法错误）。
- ✅ **M1** 后端生成链路重写：对接 ACE-Step 原生任务队列 API（`/release_task` + `/query_result`），新增「项目→乐器→take」数据模型和 `lego` 分乐器协同生成，`batch_size` 显式设为 1。
- ✅ **M2** 前端重写为剪辑软件式 UI：迁移到 React + Vite，左侧竖直图标侧栏 + 文件/生成/浏览/输出/训练/设置六个页面，canvas 波形为签名视觉元素，主色调为浅蓝，上半部分有波浪渐变装饰，侧栏文件图标上方是指挥棒+波浪的 logo。
- ✅ **M2a1**（M2 之下的一次细化调整）：
  - 「文件」页新建项目表单简化为只填「项目名称」和「乐曲总时长」，风格描述/调性/拍号/BPM 都交给「生成」页处理。
  - 添加乐器时新增「其他」选项，支持输入任意自定义乐器名，会正确带入生成提示词（后端 `get_instrument_spec` 的 fallback 逻辑已支持，这次补的是前端入口）。
  - 训练/设置/生成/浏览四个页面统一了表单字号、卡片内边距和页面留白，「设置」页去掉了过窄的 640px 宽度限制，「浏览」页波形加高、操作按钮对齐。
- ✅ **M2a2**（本次改动，M2 之下的另一次细化调整）：
  - 「生成」页头部右上角的调性/BPM/拍号 chip 全部去掉，音乐参数只在「高级」面板里看和调。
  - 「高级」模式下的调性/拍号/BPM/乐曲总时长挪到了风格描述输入框的上方（原来在下方）。
  - 风格描述输入框在没有内容时，和其它输入框一样显示占位提示文字。
- ⏳ **M3** 训练页后端：尚未开始，卡在需要确认训练机器上具体用 ACE-Step 自带的 Gradio LoRA Training 标签页，还是 Side-Step CLI。
- ✅ **M4** 手机遥控指挥（本次改动）：在原本"手机单机自采自放"之外，新增"手机当指挥棒、电脑出声"的模式，两种模式在「输出」页可切换。
  - 后端新增 WebSocket 中转 `/ws/conduct/{room}`（`backend/conduct.py`），只做纯转发不解析手势；新增 `/api/network-info`（`backend/netinfo.py`）供前端拼手机可达地址。
  - 前端把"采集传感器"抽成可替换的 `SensorSource`（`lib/sensorSource.ts`），单机模式与电脑模式共用同一套指挥/出声逻辑。
  - 「输出」页电脑模式显示房间码与二维码，手机扫码即进入专用遥控界面（`pages/RemotePage`，不加载任何音频）。
  - 开发模式下 Vite 改为监听 `0.0.0.0` 并代理 WebSocket——此前只绑 localhost，手机在局域网里根本连不上。
  - 新增 `scripts/dev-certs.sh` 生成 HTTPS 证书：iOS 只在安全上下文才允许运动传感器权限。HTTPS 是显式开关（`npm run dev:https`），避免装完证书后 http:// 旧地址静默失效。
  - 「输出」页电脑模式提供「局域网 / 隧道」两种配对方式，任何手机零安装即可用隧道方式接入。`npm run dev:tunnel` 按后缀放行常见隧道域名，解决"隧道域名随机、Vite 却要启动时就知道放行谁"的鸡生蛋问题。
  - 隧道可在 UI 上一键启停（`backend/tunnel.py` 代管 cloudflared 子进程，抓到域名后回传前端自动填入），不需要另开终端；后端退出时自动关闭隧道。
  - 顺带修回一个 M2 移植时漏掉的逻辑：起播后 5 秒无传感器数据的检测（原先桌面端会永远停在"等待手势…"）。
- ✅ **M4b** 指挥响应稳定性重构（本次改动）。修掉的是判定链路本身，不是传感器选型：
  - **力度不再取瞬时能量，改取 600ms 活动包络。** 挥拍时能量在一拍之内必然从峰值掉到零，直接拿瞬时值当力度会让所有音轨跟着拍子整体脉动 —— 这是"指挥时乐器乱响"的一大来源。逐拍重音应由节奏声部表现（M4c）。
  - **力度分母从写死的 15 改成可达的绝对区间（0.3~7.0）。** 实测手持挥拍 energy 中等幅度峰值才 3~5，原来的 15 要靠剧烈甩动才够得到，这是"动作稍微小一点就没有声音"的直接原因。判断"在不在指挥"则改用跟随包络的自适应滞回门限（30%/12%）+ 最短驻留 150ms，兼顾不同人的挥动幅度差异。力度取值保持绝对，避免自动增益把轻挥拉回满音量、丢掉强弱对比。
  - **重力基线改为自适应跟踪，不再硬编码 -9.81。** `sensor.ts` 取值是 `accelerationIncludingGravity || acceleration`，在前者为空的设备上会退到不含重力的读数，减 9.81 后恒为负、clamp 成 0 —— 那类设备上从头到尾一点声音都不会有，同时 cutoff 疯狂误触发。
  - **cutoff 判定修正。** 原来 `isRising/isFalling` 排在 cutoff 之前 return，而真正的收势必然是下降趋势，结果真收势多半被认成 decrescendo；且门槛用的绝对值（末帧模长 <1）作用在含重力的模长上，等于要求自由落体，正常设备根本不成立。现在先判 cutoff，门槛跟随包络，并要求连续低能量 ≥80ms + 500ms 不应期。
  - **收势响应改为自然衰减。** 原来是主音量瞬间归零、100ms 后无条件拉回 1，既不管用户是否还在指挥、也不管当前该是什么音量，还和每帧持续写入的 trackVolume 直接冲突 —— 这就是"完全停止之后又直接没有声音了"。现在收势走力度自己的 release 曲线，约 1 秒平滑降到保持音量（0.18）并维持，重新挥动立即恢复，全系统只有这一套衰减机制。
  - **判定窗口从帧数改成毫秒。** 原来 `historySize=60`、趋势取 30 帧，隐含 60Hz 假设；电脑模式下采样经 WebSocket 到达，网络抖动会让同样帧数覆盖忽长忽短的真实时间。
  - **音量写入合并为每轨每帧一次**，并从 `linearRampToValueAtTime` 换成 `setTargetAtTime`。原来同一帧可能对同一乐器写两次，且 50ms 的 ramp 被 16ms 一次的调用不断打断、斜率跳变，听感就是抖动和咔哒。
  - 顺带修掉一个起播 bug：`bpm` 初值写死 80 而 `baseBpm` 是之后才从项目读的，120BPM 的项目起播瞬间 tempo 会是 0.67，音乐一上来先慢三分之一。
- ✅ **M4c** 声部逻辑重构为混音平衡范式（本次改动）：
  - **拆出 `ConductIntent` 抽象层。** 手势解析分成"传感器适配器 → 指挥意图"和"指挥意图 → 各声部音量"两段，度数阈值、加速度过零点、重力基线这些 IMU 特有的东西全部收敛在适配器里。混音层 `mixIntent()` 与传感器无关，M5 的摄像头适配器产出同一个结构即可接入，声部逻辑不必重写。
  - **四轴 sigmoid 换成强调向量。** 原来 melody/harmony 同取 gamma 轴、bass/rhythm 同取 beta 轴 —— 四个"声部"其实只是两个物理轴的正负两端，既不独立也对不上音乐意义上的四类。而且手机端正握持时四者只有 0.21~0.27，正常姿势下每个声部都只有约四分之一音量；要把主旋律推到接近 1 得左倾 50° 以上。现在倾 35° 即到满强调，端正时三个声部一律回到 0.45 底噪。
  - **节奏声部改由拍点脉冲驱动**（200ms 衰减），不再占用 beta 轴负端。打击类声部本质是点状事件，跟着拍"哒"一下，不该和旋律一样是连续朝向量。
  - **删除排序静音。** 原来 density<0.3 时按当前帧四个角色的相对大小排序、把最低的两个强制静音；排序逐帧重算，稍有抖动名次就翻转，刚静音的声部下一帧又起播 —— 这是"乐器乱响"最直接的来源。现在每个声部各自独立取值，不比名次、不关声部，density 改为调节强调对比度（动作越大底噪压得越低）。
- ✅ **M4c1** 参数集中、调试页与文档同步（本次改动）：
  - 全部可调参数集中到 `frontend/src/lib/gestureConstants.ts`，每个都写了取这个数的理由。调手感只改这一个文件。
  - 新增手势解析调试页，开发模式下访问 `/?debug=conduct`（仿照已有的 `?conduct=` 走 query 分支，不进侧栏、生产构建里不可达）。用合成传感器序列驱动真正的 `GestureInterpreter`，实时画出力度、四声部音量、密度与收势触发点，预置六段序列，改完参数刷新即可对比。
  - **力度改用滑动窗口峰值。** M4c 用的指数衰减峰值跟随器在两个拍点之间仍会衰减（600ms 时间常数遇上 500ms 一拍，每拍掉到 0.435 倍），力度依然是锯齿状的 —— 调试页的曲线才看出来。换成滑动窗口取峰值后稳定挥拍的力度是真平的。
  - **修正起播基线收敛。** 重力基线原本只用第一帧初始化，若那一帧恰好落在动作的峰值上，头两秒的能量就是错的，拍点检测门限过不去、节奏声部整整两秒不出声。改为起步阶段等价于 running mean，之后交棒给慢速跟踪。
  - **修正收势判据与重复触发。** 收势的「停下了」原本用能量判，而能量要减去慢变的重力基线，刚停手时降不下来、几乎永远差一点触发不了；「刚才在挥」原本取趋势窗口前半段均值，到判定时刻已基本落在停手之后。改为「activity 高 + 模长峰峰值小」，并只在「由动转静」的瞬间报一次 —— 否则只要人保持静止，条件会一直成立、每过一个不应期就再报一次。修完停手落定时间从 1.3 秒缩到 0.87 秒。
  - **fermata 与画圈颤音标注暂缓**，并在 `MTX_PROJECT_SPEC.md` 写明原因：fermata 与「停手」在单手 IMU 上是同一个输入（都只是静止），要区分「半空中定住」和「收势停下」，模长信号给不出；画圈需要角速度积分与轨迹形状识别。两者在 M5 摄像头方案下都会容易得多。此前它们在文档里列为已设计、代码里却没有实现。
  - `MTX_PROJECT_SPEC.md` 的「手势映射设计详解」整章改写为当前实现，并给早期的 `gesture.js` 代码块加上过时警示。
- 🚧 **M4d** 构型页 + BYOK 语言模型（进行中，已完成地基与构型页）：
  - **数据地基**：项目 JSON 加 `schema_version` 与惰性迁移（`load_project`/`list_projects` 时自动补齐并回写，由显式 `DEFAULTS` 表驱动）。此前无版本号、无迁移，而读取遍地是 `project["xxx"]` 直接下标，加任何字段都会让老项目 KeyError。
  - **`total_duration` 成为正式字段**，`segment_duration` 降级为同步写入的影子副本。此前一个字段被当两个概念用：前端两处 UI 标签写「乐曲总时长」，后端却拿它当单次生成时长传给模型。
  - **`add_instrument` 支持 role/family 覆盖**。此前 role 只能从 `get_instrument_spec(library_key)` 查，自定义乐器一律退化成 `harmony` —— 构型给它们分配的声部会全部落空，指挥时四个方向的响应废掉一半。
  - **新增「构型」页**（侧栏在「文件」与「生成」之间）：用语言模型确定乐曲全局属性与情绪曲线，然后一键写回项目并创建乐器 tab。交互范式是**骨架 → 一次成型 → 局部重问**，明确不做多轮对话（对话历史与用户的手工编辑会形成双真源，下一轮会无声覆盖用户的修改）；局部重问是单次无状态改写，服务端强制只接受 `scope` 内的字段变化。
  - **乐曲情绪柱状图**：横轴时间、纵轴情绪强度，高潮段高亮。段落是唯一真源，柱状图是它的纯函数投影，高潮起止时间是 `is_climax` 段的并集（不做独立字段，否则两份数据必然漂移）。拖柱子 = 调整整段强度、整段联动并保持段内形状；段落边界是另一个手势。按小节自适应分桶、目标 40 根柱。
  - **5 个模版**（存比例不存秒数，同一模版对 16 秒和 4 分钟都适用）：史诗电影配乐、室内乐（无高潮）、进行曲（力度是台阶不是斜坡）、极简氛围（无打击乐）、圆舞曲（三拍子）。**没配 key 时整页照常可用** —— 模版、段落编辑、拖拽、乐器增删都不需要联网；模型调用失败会落地模版结果加一条警告，用户永远不会面对空白页。
  - **BYOK 语言模型**（`backend/llm.py`）：只做 OpenAI 兼容层 + 自定义 base_url，覆盖 DeepSeek/智谱/Kimi/OpenRouter/Ollama/OpenAI。key 只存后端、文件权限 600、在 `.gitignore` 里、任何接口都不回显明文（只回 `has_key` 与掩码）。base_url 做**严格主机名匹配**而非子串包含（`api.openai.com.evil.com` 与 `evil.com/api.openai.com` 都会被拒绝）。隧道运行时 LLM 接口要求本机令牌 —— 隧道会把后端暴露到公网，而房间码只保护 WebSocket 不保护 REST。
  - **永远不直接信任模型输出**：时间越界按比例缩放而非截断（模型容易按 3 分钟的常识分段却忽略你只有 16 秒，截断会把 outro 整个切掉）、权重 clamp、participation 长度对齐、乐器名三级映射（精确/别名/自定义，含中译英）、高潮不连续则合并。
  - 尚未完成：生成页的只读化改造、参与度增益包络接进指挥、双模式生成（模式二需先实测 ACE-Step `extract` 的分轨类别）。
- ✅ **M4a**（本次改动，M4 之下的一次细化调整）新增云端 API 生成方式，本机不跑模型：
  - 新增 `backend/tme_backend.py` 对接腾讯音乐天琴 workflow API（HMAC 签名、异步提交、轮询、下载），`GENERATION_BACKEND=tme` 即可切换。
  - 生成请求的 tags 会带上「生成」页里的全部音乐信息：乐器（含自定义名）、角色、调号、拍号、BPM、片段时长、风格描述、已有声部。
  - 天琴只有整曲文生乐：`lego` 降级为共享乐理上下文的文字对齐，`repaint` 明确返回 501 且前端自动隐藏按钮；「设置」页新增「当前后端能力」卡片说明差异。
  - 天琴返回整曲 MP3，用 ffmpeg 转单声道 WAV 并裁到项目时长，保证多轨对齐；`mix_wav_files` 改为跳过读不了的音轨而不是整体崩掉。

- 🚧 **M5** 摄像头指挥（进行中，基础链路已跑通）：
  - **不用手机，对着电脑摄像头直接指挥。**「输出」页新增第三种模式（单机 / 电脑 / 摄像头）。用 MediaPipe HandLandmarker 做手部追踪，纯推理、不训练。
  - **按真实指挥法的行业标准做双手分工** —— 这是摄像头相对 IMU 的核心收益，一部手机只有一只手，做不了：
    - *打拍手*（默认右手）：拍点(ictus) 取**垂直速度过零点**，即手向下走到底、开始回弹的那一瞬间。教材的三段式是 预备(prep) → 拍点(ictus) → 反弹(rebound)，拍点是运动学拐点而非位置阈值。拍型的**大小本身表达力度**（"the right hand pattern gradually gets larger, both in height and in width"），所以取轨迹包围盒当力度主项。
    - *表情手*（默认左手）：抬高是渐强、落下是渐弱（"begin on the horizontal plane and move upwards and outwards"）；横向位置按**乐队席位**决定强调哪个声部 —— 左手边第一小提琴（主旋律）、中间木管（和声）、右手边大提琴与低音提琴（低音）。
    - 只伸一只手时退化为该手兼管两者。教材虽反对左右手镜像，但也承认 "all the great conductors do it"。
    - **持棒手可左右互换**：ConductIT 明确写 "There is no right or wrong as to whether you choose to hold the baton in your right or your left hand"，所以不把「右手打拍」写死，UI 上有「左手打拍」开关。
  - **新增 `IntentSource` 抽象层**（`lib/intentSource.ts`）。M4 的 `SensorSource` 抽象的是「传感器采样从哪来」，产物是 IMU 形状的 `SensorSample`；摄像头给的是关键点坐标，硬塞进去就得伪造「度数的欧拉角」和「基线 9.81 的加速度」。现在往上提一级抽象「指挥意图从哪来」，IMU 与摄像头各自产出 M4c 定义的 `ConductIntent`，混音层对传感器一无所知。
  - **资源全部本地化，断网可用**：模型 `hand_landmarker.task`（7.5MB）进仓库；WASM 运行时不进仓库 —— 它随 `npm install` 已经在 `node_modules` 里，由 `predev`/`prebuild` 钩子自动复制（`frontend/scripts/sync-mediapipe-wasm.mjs`），不像 `dev-certs.sh` 那样要记得手动跑。只带 SIMD 版本，省掉 10MB 的 nosimd 兜底。
  - **实测性能**：稳态推理中位 **4.6ms**（首帧 163ms 是 GPU shader 预热）。摄像头本身的 30fps 才是瓶颈，推理不是。此前调研里那个「190ms 同步偏差」来自一个特定的 LSTM 实现，规则式方案在关键点上几乎零成本。
  - 合成信号验证 15 项全通过：拍点数与理论值一致、力度随拍型单调上升（0.25→0.69→1.00）、抬手渐强、席位分区准确、停手平滑落到保持音量（单帧最大跌幅 0.04）、单手退化可用。
  - 尚未完成：DTW 图形拍型识别（识别当前在打 2/3/4 拍，做成可插拔接口以便后续替换成训练好的分类器）、教学模式。
- 🚧 **M6** 两级导航 + 指挥教学（进行中，已完成字体、图标与导航）：
  - **UI 全面改衬线体**（Source Serif 4 + Noto Serif SC）。界面以中文为主，只换拉丁字体的话中文会掉回系统黑体，中英一衬一非衬会很割裂，所以 CJK 衬线是必需的而不是锦上添花。CJK 只引 400/600 两档字重：`@fontsource` 的中文包按 unicode-range 切成一百多个分片，每多一档字重产物 CSS 就多几十 KB，而 CSS 阻塞渲染（引全 4 档时 CSS 是 525KB，砍到两档后 281KB）。
    - canvas 里的刻度文字原本各自写死 `system-ui, sans-serif`，换字体时会被整片漏掉。新增 `lib/canvasFont.ts` 从 computed style 读 `--font-mono`，情绪柱状图、摄像头预览、调试页统一走它。
    - 代价是衬线体数字不等宽。DOM 里用 `font-variant-numeric: tabular-nums` 补救（见 `.mono-chip`），canvas 里补救不了。三个字体 token 相互独立，觉得 mono 不合适时改 `global.css` 一行即可退回等宽体，其余代码不用动。
  - **品牌图标更换**为 MW 字标 + 海军蓝→黑的自上而下渐变，同一份图形同时是侧栏 logo（`components/Logo.tsx`）与 favicon（`public/icon.svg`），改一处要同步另一处。补上 `apple-touch-icon.png` —— 手机端 `?conduct=` 会被存到主屏当遥控器用，而 iOS 不读 SVG favicon。
  - **导航改为两级**：一级「指挥教学 / 指挥体验」，原有七个页面归入「指挥体验」，训练与设置是不属于任何一级的工具页、固定在底部。侧栏从 64px 纯图标栏加宽到 196px 带文字 —— 两个一级项是整个应用的分岔口，光靠图标分不清，认错了就走错路。
    - **一级归属由 `activePage` 推导，不另存一份 `section`。** 存两份就得让每一处 `setActivePage` 都记得同步，而跨页跳转有好几处（打开项目、构型应用到生成页、浏览页编辑乐器），少一处就是一个「侧栏高亮和内容对不上」的 bug。侧栏另存一个 `navSection` 只用于「展开哪一级的二级列表」，跳到训练/设置时刻意不变，否则从「输出」点「设置」就回不去了。
  - **课程数据与讲解/示范**（`lib/teaching/`）：单元一二三共 11 课，按 ConductIT 的单元顺序编排 —— 先把右手拍型练到不用想，再谈两只手的独立性，最后才是力度与速度。全部是静态数据，**不配天琴密钥、不插摄像头也能把一课看完**，练习曲与打分是加分项而不是前置条件。每课含标准依据、要点、常见错误、练习曲提示词（强制 BPM 准确 + 含打击乐 + 开头一小节数拍）与评分权重。
    - **标准图形拍型的几何单独成模块**（`lib/teaching/patterns.ts`），一物三用：示范动画、跟练时的参考叠加、之后 DTW 的模板。坐标系与 `camera/conductingModel.ts` 的 `toConductorView()` 完全一致（x 是指挥自己的左→右，y 是下→上），DTW 模板可以直接和摄像头输出比对，中间不用换算。
    - **所有拍点在同一水平面上**（conducting plane）。口诀「下 → 左 → 右 → 上」说的是每一拍动作把手带向哪里，不是拍点的高低 —— 所以口诀不标在拍点上（第 4 拍叫「上」，它的拍点却在平面上），改标在两个拍点之间的箭头旁。
    - **拍与拍之间不是匀速**：离开拍点快、反弹顶点慢、再加速砸向下一个拍点。这个加速正是拍点清晰的来源，匀速走一遍看起来像机械臂，学的人也学不到「要加速下去」。
    - 修掉一个自己埋的坑：`rebound` 写着是「反弹顶点」，实现里却当二次贝塞尔的**控制点**用，而贝塞尔不经过控制点 —— 标 0.84 的顶点实际只到 0.48。把画出来的曲线量了一遍才发现，现在反解控制点让曲线真的经过它，并加了断言。
    - 示范画布上多画一条**身体中线**：二拍与三拍只在中线右侧活动，四拍才跨到左边。不画这条线的话，三拍图看起来像是「整个挤到右边去了」的排版事故。
  - **「考试」页**（`pages/ExamPage`）：用**固定的示例歌曲**打分，摄像头采集。和课程里的跟练分开是因为可比性 —— 练习曲现场生成、每次都不一样，分数没法横向比；考试用同一批曲目、同一个速度，分数才有意义，所以考试曲目不走练习曲端点而是随应用附带。三首覆盖三个难度（进行曲只考跟得上／圆舞曲考三拍型与渐强／抒情段落把弱起、延音、渐慢一次全考）。
    - `audio` 字段显式留成 `null` 表示「曲目未就绪」，UI 明说，而不是摆一个点了没反应的「开始考试」。
    - **摄像头自检是真的、现在就能用**：安全上下文与浏览器支持检查、开摄像头、认左右手、看帧率（复用 M5 的 `HandTracker` 与 `CameraPreview`）。考前该踩的坑当场踩完。
    - 及格 70 / 优秀 85。定得不高是因为初学者跟着音乐打拍时，人的听觉—动作延迟本身就有几十毫秒，要求太严会把「已经能带起乐队」的人判成不及格；真机试考后按实际分布回调。
  - **跟练录制与六维评分**。**跳过第 4 步先做的**：练习曲要配密钥、每次迭代一次云端调用加几十秒，而评分的核心是纯函数，用合成轨迹离线验证一秒一轮。更关键的是拿节拍器当拍网格反而更干净 —— 网格原点是我们自己写下的常数，不需要去检测音频起始点。
    - **严格网格的节拍器**（`lib/teaching/metronome.ts`，Web Audio 前瞻排程）兼作拍网格真源。不用 `synth.py` 的打击声部是因为它每拍有 10% 概率漏拍、随机种子还跨进程不可复现 —— 漏拍的节拍器不能拿来给人打分。
    - **网格原点补偿了输出延迟**。`AudioContext.currentTime` 是「送进音频图」的时刻，声音从喇叭出来还要晚 10~30ms；用户跟着**听到的**声音打拍，不补这一项每个人都会被系统性判成拖拍那么多毫秒，而且是个谁也看不出来的固定偏差。
    - **录制层**（`lib/teaching/recorder.ts`）逐帧存原始样本而不是算好的指标：指标算法还会改，原始数据不会。`ConductIntent` 没有时间戳、`lastIctusAt` 只留最后一次、轨迹只滚动留 1200ms，所以另开一条 `onSample` 通道，混音路径不受影响。
    - **DTW 图形拍型识别**（`lib/camera/beatPattern.ts`，M5 欠的账），做成可插拔接口。用 DTW 而不是逐点欧氏距离：同一个拍型有人第 2 拍拖一点、第 3 拍赶一点，形状是对的、时间对不齐，而时间偏差已经由另外两维单独评了，这里再罚一次就是重复计分。
    - **六个维度**（`lib/teaching/scoring.ts`），每维都给具体数字 + 一句能照着改的建议，而不是一个笼统分数 —— 文献承认人工评审的问题常常出在评语说不清楚。三处设计上的讲究：
      - **满分基准取自模板自己**。拍点清晰度拿标准拍型算出的比值当 100 分，不是拍脑袋定阈值。而且基准要**按录制的实际帧率**去采样：模板解析精度下是 2.74 倍，30fps 采样同一条轨迹只测得到 2.33 倍，拿解析值当基准等于设了条谁都够不着的线（完美动作只能得 85）。
      - **容差随速度缩放**。拍点偏差按拍长的百分比算：168 BPM 一拍才 357ms，固定 180ms 容差等于允许偏半拍。
      - **形状按用户自己的强拍切小节**，不按网格切。否则一个稳定拖拍的人，每个"小节"都是被旋转过的拍型，DTW 距离自然差 —— 时间误差会渗进形状分。
      - 速度稳定性同时看**是否跟得上音乐**：只看自洽性的话，全程稳定慢 30% 也能拿满分，而这一维的依据本来就是「跟上乐队」。
      - **评不了的维度把权重摊给其它维度**，不当 0 分算。没有音频时「力度对应」标成「待接入」，而不是冤枉用户。
    - **合成轨迹验证 28 项全通过**（另有图形拍型几何 50 项、拍网格数学 14 项）：完美动作 99 分；拖 90ms 只扣拍点准确度且建议里点明「在拖拍」；拍型左右反了只扣 DTW 维度（15 分）不牵连拍点；匀速划圈扣清晰度；拍点越打越高扣平面一致性；稳定地打半速总分 65 不及格且点明「像是在打半速」；2/3/4 拍分类器 15/15 小节全认对。
    - 顺带补齐 `ConductingModel.reset()` 漏掉的 `lastBeatAt`/`bpm`/`lastY`/`activeSince` 等（M5 就有的 bug）：教学要反复重练，不清 `lastBeatAt` 的话重练第一下会和上次最后一拍算间隔，得出一个荒谬的 BPM 混进平滑里，直接污染评分。
  - **手机传感器兜底已砍掉**，教学与考试纯用摄像头。六个维度里有三个（拍型、平面、拍点清晰度）需要手在空间里的位置，加速度计给不出来 —— 手机兜底实际只剩两维，那已经不是「同一套评分」，拿它和摄像头的分数并列是误导。写了一半的 IMU 采样通路全部回退，不留死代码。
  - **对话式 Agent 侧栏**（右侧可折叠，`components/AgentPanel` + `backend/agent.py`）：随时问指挥知识或软件操作，常驻在 App 层所以切页不丢对话。
    - **走已有的 BYOK 通路**，不新开一条。`llm.py` 新增 `chat_text()`（自由文本、**不重试** —— 对话失败让用户再问一次就行，而构型失败会落盘半残结构才值得自动重试），配置校验 / host 白名单 / 限流抽成 `_prepare_call()`，两条通路都必须过，绕不开。
    - **三类上下文都不另存一份**：软件操作读仓库里的 `docs/USER_GUIDE.md`（按 mtime 缓存，读不到就跳过而不是整个不可用）；指挥知识由前端传 `lib/teaching/curriculum.ts` 的摘要 —— 后端抄一份 Python 版必然漂移，结果就是「Agent 教的和课程教的不一样」；project 只传摘要不传全量 JSON，一个十几轨的项目光 take 历史就上千行。用户正停在某一课时，才把那一课的全文带上。
    - **防注入**：前端传来的 `system` 消息一律丢弃，系统提示只能服务端拼，否则「你现在忽略所有限制」就成了一个 HTTP 字段。系统提示里也写明项目名、乐器名是用户数据不是指令。用假的 OpenAI 兼容端点验证过：注入的 system 确实没到达模型，到达模型的只有服务端拼的 system + 用户那句话。
    - 没配 key 时面板照常打开，只是提示「去设置页配」，不会打完一段字才报错；请求失败时把问题原文放回输入框而不是清空。
    - **课程页里内嵌了第二个入口**（M6g1）。看讲解时最容易冒出问题，让人先去右边展开侧栏就打断了。两处是**同一段对话** —— 对话状态移进 store（`agentMessages` / `askAgent`），各存一份的话用户会想不起来刚才那句是在哪儿问的，「清空」也只会清掉一半。会话本体抽成 `components/AgentChat`，侧栏只剩折叠条与标题栏那层壳。
    - 本课的建议问题由课程数据拼出来，不写死 —— 写死的话加一课就得回来改一次，而且必然有人忘。刻意不把 `goal` 拼进「为什么要…？」：goal 是完整的一句话，塞进问句里语法很别扭。
  - 尚未完成：练习曲端点 `/api/practice/generate`、示例曲目音频与「力度对应」维度（计划见 `docs/M6_PLAN.md`）。

## 用云端 API 生成（不占本机显存）

ACE-Step 要在本机跑模型，消费级设备显存吃紧。可以改用腾讯音乐天琴的云端 API：

```bash
export GENERATION_BACKEND=tme
export TME_APP_ID=你的AppId
export TME_APP_KEY=你的AppKey
uvicorn app:app --host 0.0.0.0 --port 3000
```

密钥只从环境变量读，不写进仓库。配好后「设置」页会显示当前后端、是否就绪，以及能力差异。

生成时会把「生成」页里的**全部音乐信息**编进请求的 tags，包括正在生成哪件乐器（含自定义乐器名）、它承担的角色、调号、拍号、BPM、片段时长、风格描述，以及已有哪些声部。实际发出的 tags 会打进后端日志，方便核对。

### 能力差异（重要）

天琴是**整曲生成器**，只有文生乐，没有 ACE-Step 的两项能力：

| | 本机 ACE-Step | 天琴（云端） |
|---|---|---|
| 显存占用 | 本机 16GB+ | 无 |
| 文生乐 | ✅ | ✅ |
| 音频层面协同（lego） | ✅ 真的"听"已有音轨来加声部 | ❌ 降级为共享调号/拍号/速度的文字对齐 |
| 局部重绘（Repaint） | ✅ | ❌ 按钮会自动隐藏 |
| LoKr / LoRA 权重 | ✅ | ❌ |

也就是说，用天琴时**各声部之间的配合会明显弱于 ACE-Step**——它们是各自独立生成的，只靠同一套乐理参数和文字描述对齐，而不是像 `lego` 那样在音频内容层面协同。想要最好的配合效果，仍然建议用 ACE-Step（本机或 AutoDL 等租用的 GPU 机器）。

天琴返回的是完整曲子的 MP3，后端会用 ffmpeg 转成单声道 WAV 并裁到项目的「乐曲总时长」，保证多轨能对齐循环播放。没装 ffmpeg 时会告警并保留原始音频（浏览器仍能播，但 Python 侧的混音会跳过这条轨）。

## 写谱演奏模式（M7）

前两种模式都是**直接产音频**，问题在于这类模型输出的是频谱不是音符：说了 92 BPM 出来的未必真是 92 BPM，多件乐器各生成一次就更对不齐，而这个软件恰好需要「每件乐器一条独立音轨 + 精确的拍网格 + 乐器能按段落进出场」。

第三种模式换个路子：**AI 先写谱，再由采样器演奏**。产物仍然是每件乐器一个 WAV，所以浏览页、输出页、摄像头指挥一行都没改。新建项目时选「写谱演奏」即可。

它顺带解决了几件旧问题：各声部长度精确一致、循环无接缝（尾音会叠回开头）、构型里的 `participation` 第一次真正生效（不参与的小节根本不写音符）、Repaint 真的能用（重写那几小节的音符再整轨重渲染）。谱子可以导出成 `.mid` 拿去 MuseScore 或 DAW 里用。

### 作曲器

| 值 | 说明 |
|---|---|
| `algorithmic` | 纯规则，不联网、不吃显存。调性统一、声部进行平稳、音区不重叠 |
| `llm` | 走已有的 BYOK 通路。任何一步失败都退回 `algorithmic`，并把降级原因记进 take |
| `remote` | 外部符号音乐模型服务。**失败不退回规则作曲**，直接报错 —— 选了模型却拿规则的结果糊弄过去，你就再也判断不了这个模型行不行 |

默认 `auto`：配了语言模型就用 `llm`，否则 `algorithmic`。三者都在「设置」页可切，选择存后端偏好文件。

`remote` 的接口契约见 **[docs/SYMBOLIC_COMPOSER_API.md](docs/SYMBOLIC_COMPOSER_API.md)** —— 一个
`POST /compose_part`，请求给蓝图、乐器音域与已写好的其它声部，响应回一张音符表。任何符号
音乐模型（Anticipatory Music Transformer、MMM 之类）自己包一层薄服务就能接进来，后端零改动。
和弦走向与段落结构一律本地算：符号模型擅长续写音符，不擅长按段落强度规划全曲结构，而后者
恰好是纯规则做得又快又稳的部分。

服务地址在「设置」页填，**只接受本机或局域网地址**。这条限制的对象是隧道：`tunnel.py` 一开，
后端就暴露在公网，而这个地址决定了后端把整份乐谱 POST 到哪去。要连公网服务请用
`SYMBOLIC_COMPOSER_URL` 环境变量启动 —— 那是启动时的运维决定，不是能被远程改写的运行时开关。

```bash
export SCORE_COMPOSER=algorithmic   # 强制用本地规则，一次外部调用都不发
```

### 音源

**开箱即用，不需要装任何东西。** 仓库自带一个 33MB 的管弦乐音源
（`backend/soundfonts/orchestral.sf2`，从 MIT 授权的 FluidR3_GM 裁出来的），
由 `backend/sf2.py` 直接读取播放 —— 那是个纯标准库的 SoundFont 解析器加采样
播放器，没有任何外部可执行文件，源码跑和打包跑完全一致。

| 音源 | 说明 |
|---|---|
| `sf2`（默认） | 自带的采样播放器，真实乐器录音 |
| `fluidsynth` | 外部合成器，同一份音源，混响更丰富。装了才能选 |
| `builtin` | 纯加法合成，连音源文件都不需要。音色朴素，是最后的保底 |

在「设置」页可以随时切换，也可以用 `SCORE_RENDERER` 环境变量指定。
`/api/health` 的 `score` 块会如实报告当前实际生效的是哪个。

换自己的音源：把任意 `.sf2` 放进 `backend/soundfonts/` 即可（文件名随意，后端
自己扫）。**只支持未压缩的 `.sf2`** —— `.sf3` 的样本是 Ogg Vorbis 压缩的，
纯 Python 解不了。想换个更大的音源再裁一遍：

```bash
python3 scripts/trim_soundfont.py 完整音源.sf2 backend/soundfonts/orchestral.sf2
```

发布包里连 fluidsynth 一起带（macOS 实测 19 个文件 5.7MB，CI 会把库路径改写成
`@loader_path` 相对引用，到没装 Homebrew 的机器上也能跑），所以双击启动的用户
三种音源都能选。

**打击乐限定为管弦乐编制** —— 大鼓、小军鼓、吊镲、三角铁，没有踩镲和嗵鼓（那是爵士鼓组）。定音鼓是有音高的乐器，走普通通道写主音／属音，不当鼓组音效。

## 代码结构

- backend
  - app.py：FastAPI 入口与 REST API
  - generator.py：ACE-Step 客户端与生成任务封装
  - project.py：项目/乐器/take 数据模型
  - project_gen.py：分乐器协同生成与 lego 编排逻辑
  - generation_backend.py：生成后端抽象与能力声明
  - tme_backend.py：腾讯音乐天琴云端生成后端
  - conduct.py：手机遥控指挥的 WebSocket 中转
  - netinfo.py：局域网地址探测
  - tunnel.py：代管 cloudflared 隧道进程（UI 一键启停）
  - synth.py：程序化音频 fallback
  - agent.py：对话式 Agent 的上下文拼装（读 docs/USER_GUIDE.md，课程知识由前端传）
  - config.py：配置项与乐器目录
- frontend
  - src/App.tsx：页面路由与侧栏入口
  - src/pages：「指挥体验」下的文件、构型、生成、浏览、输出五页，「指挥教学」下的 TeachPage，以及不属于任何一级的训练、设置；外加手机端遥控页 RemotePage
  - src/components：波形、侧栏（两级导航）、右侧 Agent 面板、情绪柱状图、摄像头预览、图形拍型示范、讲评报告、二维码等 UI 组件
  - src/lib：音频引擎、传感器源、手势解析、摄像头指挥与 DTW 拍型识别（camera/）、教学（teaching/：课程数据、图形拍型、节拍器、录制层、六维评分）、指挥链路（conductLink）与 API 封装
  - src/styles/global.css：设计 token（配色、三个字体 token）；canvas 里的字体经 src/lib/canvasFont.ts 读同一份 token
- docs
  - M6_PLAN.md：M6（两级导航 + 指挥教学）的实施计划与进度
  - USER_GUIDE.md：用户使用指南
- scripts
  - dev-certs.sh：生成开发用 HTTPS 证书（iOS 传感器权限需要）
- output
  - projects：项目化生成产物
  - sessions：旧版 legacy 生成产物

## 快速开始

### 1. 后端

要求：Python 3.10+，建议在虚拟环境中运行。

```bash
cd backend
python -m pip install -r requirements.txt
uvicorn app:app --host 0.0.0.0 --port 3000
```

也可以在项目根目录以包模式启动：

```bash
python -m pip install -r backend/requirements.txt
uvicorn backend.app:app --host 0.0.0.0 --port 3000
```

### 2. 前端

```bash
cd frontend
npm install
npm run dev
```

随后访问 Vite 提供的地址即可看到界面。默认开发环境会把 /api、/audio、/project-audio 代理到后端。

### 3. 生成与演绎

1. 打开“文件”页，新建项目（只需要项目名称和乐曲总时长）。
2. 在“生成”页填写风格描述、按需调整调性/拍号/BPM，为不同乐器创建 tab（含自定义乐器）并逐个生成音频。
3. 在“浏览”页试听与检查生成结果。
4. 在“输出”页选择指挥模式：单机模式用手机打开同一地址，电脑模式用手机扫二维码接入（详见下面「手机指挥」）。

## 手机指挥

「输出」页有两种模式：

| 模式 | 谁采传感器 | 谁出声 | 需要什么 |
|------|-----------|--------|----------|
| 单机模式 | 手机 | 手机 | 手机能打开页面即可 |
| 电脑模式 | 手机 | 这台电脑 | 手机和电脑在同一局域网 |

电脑模式的流程是：「输出」页切到电脑模式 → 页面显示房间码和二维码 → 手机扫码进入遥控界面 → 电脑点「开始指挥」→ 挥动手机，声音从电脑放出。手机端只发传感器数据，不下载音频。

电脑模式下手机怎么连过来有两种方式，在「输出」页可以切换：**局域网**（延迟最低，iPhone 需装一次证书）和**隧道**（零安装，不要求同一网络）。

### 方式一：局域网 —— iOS 必须先启用 HTTPS

iOS 只在安全上下文里才允许 `DeviceMotionEvent.requestPermission()`。手机通过局域网 IP 走 http:// 访问时，Safari 连权限弹窗都不会出现。生成证书：

```bash
bash scripts/dev-certs.sh
```

脚本优先用 mkcert（推荐 `brew install mkcert`，签出的证书受系统信任），没有则用 openssl 自签名兜底。证书写到 `frontend/certs/`。

证书生成后 HTTPS **不会自动启用**——要用 HTTPS 启动（注意在 `frontend/` 目录下）：

```bash
cd frontend && npm run dev:https
```

> 换了 Wi-Fi 之后局域网 IP 会变，而证书里签的是生成时那一刻的 IP，手机会因证书不匹配连不上。重跑一次 `scripts/dev-certs.sh` 再重启即可（根证书不用在手机上重装）。「输出」页的局域网模式会自动检测这种情况并给出可复制的命令。

日常桌面开发继续用 `npm run dev`（HTTP）。这样区分是因为：如果"有证书就自动切 HTTPS"，那么所有 `http://localhost:5173` 的旧地址会静默失效，Safari 只会报一句「服务器意外中断了连接」，很难联想到是协议变了。

用 mkcert 时手机还需要安装一次它的根证书（脚本会打印步骤）；用自签名证书则手机首次访问时点「继续访问」即可。

> 每台要连的 iPhone 都得装一次 mkcert 根证书——它是只有你自己设备信任的私有 CA。如果要给多个人/多台手机演示，用下面的隧道方案更合适（公开受信任证书，任何设备零安装）。Android 上 Chrome 通常不强制 HTTPS，直接用 HTTP 即可。

Android 上 Chrome 通常不强制这一点，HTTP 也能拿到传感器数据。

### 方式二：隧道（不用证书，任何手机零安装）

「输出」页电脑模式里可以把配对方式从「局域网」切到「隧道」，页面会给出完整步骤。原理是用 cloudflared / ngrok 把本机暴露到公网，它们自带**公开受信任**的 HTTPS 证书，所以任何手机都不用装任何东西，也不要求和电脑在同一个网络。

前提是装了 cloudflared，并且 dev server 用 `dev:tunnel` 启动（原因见下）：

```bash
brew install cloudflared
cd frontend && npm run dev:tunnel
```

然后在「输出」页 → 电脑模式 → 隧道，**点「启动隧道」**即可：后端会代管 cloudflared 进程，拿到域名后自动填进输入框、二维码同步刷新，不需要你另开终端。再点一次「停止隧道」关闭。

走隧道时**不需要 mkcert 证书，也不需要 `dev:https`**——TLS 由隧道那一端终结。

其它情况：
- 没装 cloudflared 时，UI 会显示安装提示和可复制的手动命令，跑完把网址粘进输入框也一样能用（地址存在 localStorage，刷新不丢）。
- 直接用隧道域名在电脑上打开本页面时，UI 会自动识别并预填。
- 后端进程退出时会自动关掉它启动的隧道，避免机器在你不知情的情况下一直暴露在公网。

**为什么必须用 `dev:tunnel`**：Vite 会拒绝 Host 头不在允许列表里的请求（防 DNS 重绑定攻击），而 cloudflared 的临时隧道每次启动都是随机域名，没法提前写进配置——鸡生蛋。`MW_TUNNEL=1`（即 `dev:tunnel`）按**后缀**放行 `.trycloudflare.com`、`.ngrok-free.app`、`.ngrok.io`、`.ngrok.app`、`.loca.lt`，既不用提前知道具体域名，也保留了对其它域名的防护。

自建隧道或自有域名用 `MW_ALLOWED_HOSTS=a.example.com,b.example.com npm run dev`；`MW_ALLOWED_HOSTS=*` 可以放行一切，但会关掉 DNS 重绑定防护，只在临时演示时用。

> ⚠️ 隧道地址是**公网可访问**的，拿到链接的人都能接进房间指挥。房间码是 6 位随机码（约 8.8 亿组合，暴力猜不现实），但链接本身别乱发，演示完记得关掉隧道。

### 两种方式怎么选

| | 局域网 | 隧道 |
|---|---|---|
| 延迟 | 最低 | 多几十毫秒 |
| iPhone 要装证书 | 是（每台一次） | 否 |
| 要求同一 Wi-Fi | 是 | 否 |
| 适合 | 自己开发调试 | 给别人演示、多台手机 |

## 配置说明

后端配置主要在 [backend/config.py](backend/config.py) 中，支持通过环境变量覆盖：

- GENERATION_BACKEND：生成后端，local（默认，本机 ACE-Step）/ tme（腾讯音乐天琴云端）/ cloud（预留）
- ACESTEP_API_URL：ACE-Step 服务地址，默认 http://localhost:8001
- TME_APP_ID / TME_APP_KEY：天琴的 AppId 与密钥，用 tme 后端时必填
- TME_API_URL：天琴接口地址，默认测试环境
- PROJECTS_DIR：项目产物目录，默认 output/projects
- OUTPUT_DIR：legacy 产物目录，默认 output/sessions
- LOKR_WEIGHTS_DIR：LoKr / LoRA 权重目录
- ALLOW_SYNTH_FALLBACK：是否启用程序化占位音频 fallback

## 主要 API

当前主流程以 project API 为主：

- GET /api/health：检查后端与 ACE-Step 可达性
- GET /api/network-info：局域网地址，「输出」页用它生成手机扫码地址
- WS /ws/conduct/{room_id}?role=stage|remote：手机遥控指挥的中转通道
- GET /api/tunnel、POST /api/tunnel/start、POST /api/tunnel/stop：cloudflared 隧道启停与状态
- GET /api/lokr：列出可用 LoKr / LoRA 权重
- POST /api/projects：创建项目
- GET /api/projects：列出项目
- POST /api/projects/{project_id}/instruments：新增乐器
- POST /api/projects/{project_id}/instruments/{instrument_id}/generate：生成单乐器音频
- POST /api/projects/{project_id}/instruments/{instrument_id}/repaint：局部重绘

此外，仓库中仍保留了旧版 legacy 生成接口，供兼容与参考使用。

## 说明


## License

本项目采用 [MIT License](LICENSE)。

Copyright (c) 2026 Diankun Gao, Xingwen Zhao

第三方资产：`backend/soundfonts/orchestral.sf2` 由 [MIT 授权的 FluidR3_GM](https://github.com/urish/fluidsynth-manager/blob/master/fluid/FluidR3_GM.sf2)
裁切而来（见「音源」章节），其许可仍遵循原作品条款。
- 如果 ACE-Step 服务未启动，应用仍可走 fallback 流程，便于前后端联调。
- 如果需要在手机上使用传感器功能，建议使用 HTTPS 环境，例如通过 mkcert 或 ngrok 暴露本地服务。

