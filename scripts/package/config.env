# ============================================================
# MaestrWave 可选配置（KEY=value 格式，两种启动器都会自动读取）
# 用任意文本编辑器修改本文件后保存，重新双击启动器即可生效。
# 以 # 开头的行是注释；留空 = 使用默认行为。
# ============================================================

# ---- 音频生成后端 ----
# 留空：默认 local —— 连不上本机 ACE-Step 时自动用程序化占位音频，
#       完整交互/指挥链路依然可用（演示零配置）。
# tme ：腾讯天琴云端生成（不吃本机显存，效果最好），需填下面三个 TME_* 项。
# GENERATION_BACKEND=tme

# 腾讯天琴 API 地址（默认即可）
# TME_API_URL=https://test.y.qq.com/opentest/rpc_proxy/fcgi-bin/music_open_api.fcg
# TME_APP_ID=你的APP_ID
# TME_APP_KEY=你的APP_KEY

# ---- 服务端口 ----
# PORT=3000
